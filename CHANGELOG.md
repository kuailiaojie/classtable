# Changelog

## 2.0.0 (2026-09-26)

2.0.0 系列的正式版:日程有了到点提醒与桌面小组件,「日程」与「倒计时」从此各司其职。

### 日程
- **「日程」与「倒计时」分工明确**。过去两个页签是同一份数据的两种看法,看哪个都一样;现在按**分类**分流:
  **日程**页签收待办 / 活动(有时间要做的事),铺日历周条与按天的时间线;**倒计时**页签收考试 / 作业
  (等着倒数的目标),按「还剩几天」排;「其他」两边都出现。从倒计时页签点「＋」新建时默认分类是「考试」,
  从日程页签进来默认「待办」—— 少改一次分类。
- **日程到点提醒**。每条日程可单独开启提醒并选提前量(准点 / 5 / 10 / 15 / 30 / 60 分钟);全天日程
  在当天设置里的固定时刻提醒(默认 09:00)。提醒与它落在哪个页签无关 —— 一条考试只要开了提醒照样会响。
  触发时重新读一遍条目,改过标题 / 地点就显示当下的;点击通知直达那条日程。默认关闭,不设就不打扰。
  总开关与全天提醒时刻在「设置 → 提醒 → 日程提醒」。

### 小组件
- **新增「日程」小组件(4×2)**。一列近期日程与倒计时:今天的定时条目写「HH:MM」,更远的写「M/D」,
  全天写「全天」,右边标「还有 N 天」;正在进行中的那条带强调色左边条。已结束的条目不出现在这里。
  它和另外三款共用同一套选项(时刻 / 地点 / 最多显示几条 / 行距 / 底色),改完立刻刷新,日程一改组件跟着变。
- 「设置 → 桌面 → 桌面小组件」的「添加到桌面」里多了「日程」一项。

### 提醒
- 日程提醒独立一个通知渠道(「日程提醒」,高优先级),与课程提醒互不覆盖。
- 日程与课程共用同一套排程与滚动窗口:8 天窗口 + 次日 00:05 自续期,开机 / 改时区 / 应用更新后一并重排;
  两者用不同的 action 排闹钟,不会互相顶掉。

### 数据
- 日程新增 `remindEnabled` / `remindLeadMinutes`(本机库 v9 → v10 只加列,云端 Firestore 同名字段)。
  老日程保持关闭、不回填 —— 提醒是用户自己选的打扰,升级不该替所有人把闹钟打开。
- 日程提醒的两个设置(总开关、全天提醒时刻)只存本机,不参与云端设置同步。

### 文档
- README 与架构文档同步:日程 / 倒计时分工、日程提醒、「日程」小组件、本机库版本 v10。

### 版本
- 2.0.0-rc8(60) → 2.0.0(61),转正式版。

## 2.0.0-rc8 (2026-09-26)

> 预发布版(RC)。课程配色重写:取色改到感知均匀的 OKLCh(相邻两色的色差从此处处相等),
> 色相钉在课程上不再撞色,并可在设置里一键重排。

### 课表
- **课程块的颜色重写**。上一版按课名哈希取一个 HSL 色相、色相池固定 8 / 12 / 16 ——
  但 `hsl(hue, 0.30, 0.955)` 这个位置实际只能挤出约 0.01 的彩度,一屏七天看过去几乎全是白纸,
  「分不清」是必然的。现在改在 OKLCh(感知均匀的色彩空间)里取色:**明度与彩度固定、只由色相区分**,
  相邻两色在感知上的距离因此处处相等。彩度取在「整圈色相都还落在 sRGB 内」的上限之下,
  没有任何一个色相被色域裁掉 —— 一旦被裁,它与邻居的距离就会缩水,变成「有的分得开、有的几乎一样」。
  实测相邻两色的感知色差从约 0.012 提到 0.030(标准档)/ 0.041(柔和)/ 0.024(丰富),
  同时课程块上的 10sp 小字仍守住 4.5:1 的对比度。
- **色相钉在课程上,不再撞色**。新建 / 导入课程时按当前方案给它分配一个**未被占用**的色相,
  并存进课程本身(与「课程时刻一旦导入就钉死」同一个约定):课表里不同课程不会再分到同一个颜色,
  以后增删课程也不会把已有课程的颜色带跑。同一门课分成多条记录(周一一条、周五一条)仍共用一个色相。
  实测 16 色相、10 门课时,过去「至少两门同色」的概率是 97%,现在是 0。
- 「设置 → 课表 → 课表显示 → 配色方案」新增**色板预览**(当前方案铺开的整圈淡彩),以及
  **「重新配色全部课程」**:一键按当前方案把所有课程重排一遍 —— 老课表升上来后点一次,
  已经撞在一起的课就分开了。自己指定过颜色的课程保持不动。
- 三档方案改为只调彩度与色相数:柔和 8 色(最淡、相邻两色分得最开)/ 标准 12 色 /
  丰富 16 色(最显、能排下最多课程);色相数就是「能有多少门课各拿一个颜色」的容量。

### 数据
- 课程新增 `colorHue` 字段(本机库 v8 → v9 只加列,云端 Firestore 同名字段)。
  老记录保持为空、渲染时仍按课名现算,**不回填**:点一次「重新配色」就能拿到一份互不重复的分配。

### 版本
- 2.0.0-rc7(59) → 2.0.0-rc8(60)。

## 2.0.0-rc7 (2026-09-26)

> 预发布版(RC)。日视图不再把日期显示两遍;课程配色更好区分不同课程(20 个预设色 + 三档配色方案);
> 设置页重排成「分组入口 + 三级页」。

### 课表
- **修掉日视图日期显示两遍**。周 / 日合并成一页后,页面顶部的表头在日模式下会画一遍「正在看的那一天」,
  而日列表自己也画了一遍,于是同一天出现两次。现在日模式不再画这行表头,「第 N 周 · 起止」交给日列表
  随翻页一起走;周模式仍显示今天,不变。
- **课程配色更分得开**。自动淡彩的色相池按方案取 8 / 12 / 16 个和色,色相越多越不容易撞色;
  「设置 → 课表 → 课表显示 → 配色方案」里可选**柔和 / 标准 / 丰富**(默认标准),改完立即对课块、
  课程列表、详情与日程分类色生效;柔和档彩度更低、丰富档稍显。
- **课程预设色从 8 个扩到 20 个**(新增蘇芳 / 茜 / 桜 / 牡丹 / 桔梗 / 群青 / 常磐 / 萌黄 / 山吹 / 蜜柑 /
  銀鼠 等),课程编辑页的取色区自动换行。

### 设置
- **设置首页改为分组入口**。原先一长串设置项收进八个「设置中心」——外观 / 课表 / 导入与识别 / 提醒 /
  雨课堂 / 桌面 / 账号与数据 / 关于;点进分组才是原来的各项(作息时间、学期周次、调休课表、课表显示、
  AI 密钥、课程提醒…),它们各自仍是有独立页面的三级页。首页从此不会随功能增加而越来越长。
- 分组页沿用同一套 `SettingsSection` / `SettingRow`,顶栏带返回;原有各设置页与其深层页面
  (雨课堂登录 / 绑定等)不受影响。

### 文档
- README 同步新的设置路径(如「设置 → 课表 → 作息时间」「设置 → 关于 → 检查更新」)与配色说明。

### 版本
- 2.0.0-rc6(58) → 2.0.0-rc7(59)。

## 2.0.0-rc6 (2026-09-26)

> 预发布版(RC)。「新建 / 编辑日程」从底部弹层改成独立整页。

### 日程
- **「新建 / 编辑日程」改成独立整页**(顶部返回 + 内容滚动 + 底部按钮钉住),与「添加课程」同一套做法。
  原因是底部弹层走 Compose `Dialog`,其内容在部分版本里拿不到「一屏」的高度约束 —— 面板会被内容撑到屏幕外,
  底部「创建日程」按钮掉到屏幕下方,既点不到也滚不到。换成整页后不再依赖 Dialog 的测量,问题根除。
- 下线 `YohakuSheet` / `AgendaSheet` 弹层方案。字段与交互保持不变:标题、分类、全天开关、开始与结束
  (日期 + 时间)、时长提示、地点、备注、优先级;编辑态可删除。

## 2.0.0-rc5 (2026-09-26)

> 预发布版(RC)。用真机量到的实际尺寸定位并修好「新建日程」按钮掉出屏幕。

### 日程
- **修好「创建日程」按钮被裁在屏幕外**。真机量到:屏幕高 2728px,对话框内容是从**状态栏下方**(y=135)
  开始排的,却仍按整屏给出最大高度 —— 于是面板容器底边落到 y=2861,比屏幕低 133px,面板(0.9 屏高)连同
  底部按钮一起被裁在屏幕下方(所以点不到、也滚不到)。
  现把面板容器上限由「整屏高」改为「整屏高 − 状态栏高」,容器正好落在屏幕内:面板 0.9 屏高、
  中间可滚动、按钮钉在底部并让开导航栏与键盘。

## 2.0.0-rc4 (2026-09-26)

> 预发布版(RC)。再修「新建日程」面板:把 Dialog 窗口钉成整屏。

### 日程
- **彻底修好「创建日程」按钮掉出屏幕**。rc3 只把面板容器钉到「屏幕高」,但 `Dialog` 窗口本身的可用高度
  小于一屏,容器仍然比窗口高、底部被裁,按钮掉到屏幕下方且点不到。现直接把窗口钉成整屏
  (`window.setLayout(MATCH_PARENT, MATCH_PARENT)`,新版 Compose 对高内容也是这么做兜底的),
  内容这才拿到「正好一屏」的硬约束:面板 0.9 屏高、中间区域可滚动、按钮钉在底部并让开导航栏与键盘。

## 2.0.0-rc3 (2026-09-26)

> 预发布版(RC)。纠正 2.0.0-rc2 没治好的「新建日程」按钮问题。

### 日程
- **真正修好「创建日程」按钮掉到屏幕下面、面板滚不动**。根因是面板所在的 `Dialog` 窗口按 `WRAP_CONTENT` 测量,
  内容拿到的是**不封顶的高度约束**,导致 `fillMaxSize` / 面板的 `fillMaxHeight(0.9f)` / 子项的 `Modifier.weight`
  全部失效 —— 面板被内容撑到比屏幕还高、按钮被顶到屏幕下方,中间也没有可滚动的剩余空间(所以拖不动、点不到)。
  现把面板根容器显式钉到「最多一屏」(`heightIn(max = 屏幕高).fillMaxHeight()`)再填满,约束重新成立:
  面板恢复 0.9 屏高、中间区域可滚动、按钮钉在最底部并让开导航栏与键盘内边距。

## 2.0.0-rc2 (2026-09-26)

> 预发布版(RC)。修掉 2.0.0-rc1「日程」页的两处交互问题。

### 日程
- **日历周条改为左右拖动翻周**。去掉两侧的 `‹ ›` 箭头,直接横向拖动换周(与课表页同一套手势);
  换周后选中日平移到同一星期几,时间线跟着周条走;「今天」仍动画滚回本周并选中今天。
- **修复新建日程面板底部「创建日程」按钮够不到**。键盘弹出或有手势导航栏时,按钮被压到屏幕下沿、
  滚也滚不到;面板现在让开「导航栏」与「键盘」中更高的那个,按钮始终贴在键盘上方。

## 2.0.0-rc1 (2026-09-26)

> 预发布版(RC)。本机编译与构建已通过。这一版把「日视图」并进了「课表」、空出的位置给了全新的
> 「日程」,并给全应用铺了一套统一的动效。

### 课表:周视图与日视图合并
- **右上角切换周 / 日**。以前周视图、日视图是两个底部标签页;现在合成一页,右上角「周 / 日」即时切换。
  周模式一屏看全一周、左右滑翻周;日模式一天一页、左右滑翻日。「今天」按当前模式跳回。
- **左上角当前天气**。天气从日视图的卡片变成课表表头左上角的一个**图标 + 温度**(点一下弹出完整天气卡,
  并刷新一次);还没有数据时不占位。温度变化时数字滚动,不再硬跳。
- **底部导航改为 课表 / 课程 / 日程 / 设置**。日视图不再是独立标签页。

### 课程块自定义(设置 → 课表 → 课表显示)
- **显示内容**:时刻 / 节次 / 地点 / 教师,想显示哪几项勾哪几项(课块够高才多显示一行)。
- **行高密度**:宽松 / 标准 / 紧凑,决定一屏能放下多少节次(紧凑档在小屏可能需要上下滚动)。
- **课程名**:1 / 2 行、小 / 中 / 大字号。
- **块样式**:圆角(方 / 中 / 圆),以及是否铺课程淡彩底(关掉就是纯文字块)。
- 选项存本机、与「桌面小组件」同一套做法,改完立即生效,不参与云同步。

### 日程(新)
- 新增「日程」标签页,一份数据两种看法:**议程**(日历周条 + 按天时间线)与**倒计时**(按剩余天数排序的列表),右上角切换。
- **日历周条**带农历:`LunarDate`(基于 `cn.6tail:lunar`)给出节气 → 节日 → 农历日名,如「秋分」「中秋节」「十六」;
  今天 accent 实底、选中日 accent 描边。
- **议程时间线**:全天条目置顶,其余按 上午 / 下午 / 晚上 分段,每条给出起止时刻、分类、地点、备注。
- **新建 / 编辑面板**:标题、分类(待办 / 活动 / 考试 / 作业 / 其他)、全天开关、开始与结束(日期 + 时间,自绘选择器)、
  时长提示、地点、备注、优先级(无 / 低 / 中 / 高),编辑态可删除。
- **倒计时列表**:进行中的按剩余天数排序,已结束的折叠在后(逾期 N 天)。
- **云同步**:与课程同一套约定 —— `users/{uid}/agenda` + `users/{uid}/deleted_agenda` 墓碑,LWW 合并、删除不复活。
  未登录仍是纯本地。数据库 v7 → v8(新增 `agenda_events` / `deleted_agenda` 两张表)。
- 说明:本版日程**不做**到点提醒。

### 动效
- 新增 `design/YohakuMotion.kt`:统一的时长与缓动 token(取自 GSAP 的运动原则 —— `power2.out` /
  `expo.out` / `back.out` 对应成三次贝塞尔),以及错峰(`stagger`)与常用 spring。
- **导航转场**:根标签之间淡入 + 轻微缩放;二级页共享轴从右滑入、返回向右滑出。
- **开屏**:分成时间线的入场(logo → 标题 → 副标题依次错峰)与放大淡出。
- **底部导航**:选中态文字颜色与指示点带动效,按压缩放。
- **弹窗 / 底部面板**:入场轻微放大 + 淡入(面板从底部滑入)。
- **课程块**:切周 / 切日时按序号错峰淡入放大;「正在上」的 accent 条有呼吸;「现在」时间线位置带缓动。
- **切周视差**:横向拖动时表头比网格移动得稍慢一点。
- **按压反馈**:由瞬时压深改为带缓动的淡入淡出。
- **列表项**:日程时间线 / 日列表 / 课程列表入场错峰,条目增删带动画(`animateItem`)。
- 纪律:只动 alpha / translation / scale(合成层),不动会触发布局的属性;时长普遍落在 180–460ms。

### 版本
- 1.1.0-rc1(52) → 2.0.0-rc1(53)。

## 1.1.0-rc1 (2026-09-25)

> 预发布版(RC)。本机编译与构建已通过,但「上课免打扰」要改的是系统状态 —— 进出的时机、
> 权限引导、和你自己开的免打扰会不会打架,都得在真机上跑一天才算数。确认没问题后再发正式的 1.1.0。

这一版补上课期间的免打扰,并把更新相关的开关从设置页收进「检查更新」页。

### 免打扰
- **上课自动进免打扰,下课自动退**。按课表作息来:有课的那一段进免打扰,课间退出;连着上的课
  算作一段,中途不会退出。调休日停课不生效,补课日照常生效。开关在「设置 → 提醒 → 上课免打扰」。
- 用系统**勿扰访问权限**切换(不是 AutomaticZenRule):因此页面上直接给出授权入口,
  从系统设置回来会刷新状态。没授权时功能空转,不会自作主张。
- **不抢用户自己的免打扰**:进入时如果手机本来就已经是免打扰,应用不接管(退出时自然也不会
  替你关掉);只有由应用切换的那一次,下课才按原样恢复。
- **时间重叠的课不会被提前放行**:A(8–10)与 B(9–11)同时上时,A 到点下课的闹钟会先确认
  此刻还有没有别的课,有就继续免打扰,不会把 B 的上课时间放响。
- 排程并进课程提醒那一套:同一个 8 天滚动窗口、同一份「已排台账」(所以删课会连带撤销)、
  开机 / 改时区 / 应用更新后一起重排,12 小时自愈任务也一并兜底。重启后丢掉接管标记,
  免得一次迟到的「下课」闹钟把早已过期的档位写回去。

### 更新
- **更新选项收进「检查更新」页**:自动检查更新的开关、检查频率(每天 / 每 3 天 / 每周)、
  是否接收预发行版(RC),现在都在这一个页面里;设置页「关于」只留一个「检查更新」入口,
  以后不用再在设置里翻。
- 频率只管后台检查:后台任务仍是每天跑一次,由它决定「隔几天才真查」;手动「检查更新」
  不受频率影响。

### 版本
- 1.0.1(51) → 1.1.0-rc1(52)。

## 1.0.1 (2026-09-24)

- **全新开屏动画**：以应用角色图、淡入缩放和品牌文案组成轻量首屏过渡，随后自动进入课表，不影响后续加载。
- **课程颜色自定义**：编辑课程时可从 8 个预设色中选择，也可以输入 `#RRGGBB`；颜色会随课程本地保存并同步到其他设备，留空则继续使用自动淡彩。
- **应用图标设置完善**：保留 9 张角色图、手动选择与轮播功能，图标切换不影响应用进程。
- **版本号**：`1.0.0 (50)` → `1.0.1 (51)`。

## 1.0.0 (2026-09-24)

1.0.0 系列的正式版:rc1、rc2 做完的改名与图标、按架构拆包、预发行版更新、设置拆页与官网
都就此定稿,并修掉小组件「紧凑」把字压小的问题。

### 小组件
- **修「紧凑」把字号压小**。「设置 → 桌面小组件 → 行距」里选「紧凑」,本意是让课表排得更密,
  实现上却把字号也各降了一档:课程名 12 → 11sp、时刻 11 → 10sp、「下节课」的课名 14 → 13sp。
  于是选了紧凑的人看到的是**字变小**,而不是行变密 —— 这个选项本来就该只管行距。
- 现在紧凑只收紧排布(课表行距 4 → 2dp、内边距 12 → 8dp),字号保持 11–14sp 不变,和它的
  名字对上。默认的「标准」字号从来没变过,所以不开紧凑的人观感与上一版完全一致。

### 官网
- **超过反代上限的安装包不再下载失手**。反代(Netlify Function)单次响应约 5MB 封顶,超限的整包
  请求会被回 409,浏览器原生下载就只会存下一个 JSON 错误。现在官网自己按 4MB 分段取回(与
  Android 端同一套约定),拼成一个文件再存盘,进度就写在下载按钮旁边;不超过上限的小包仍旧
  交给浏览器下载,进度与续传都是现成的。
  - 存盘文件名取 Release 资产的原名(反代新透出 `Content-Disposition`),下载目录里认得出是
    哪个架构的包。
- **静态资源加缓存破冰**。`_headers` 给 `/assets/*` 定了一年的 immutable 缓存,而引用它们的
  URL 一直没变,于是改了样式或脚本、回访用户一年内跑的还是旧版;现在样式与脚本的引用都带
  `?v=`(改完 assets 就加一)。
- 顺带修掉 hero 区块比别的区块多出 24px 横向内边距的问题(`padding` 简写把 `.container` 那层
  横向留白一起清零了)。

### 版本
- 1.0.0-rc2(49) → 1.0.0(50),转正式版。

## 1.0.0-rc2 (2026-09-24)

> 预发布版(RC)。本机编译与构建已通过。这一版把上一版没来得及做的几件事补上:
> 更新能收预发行版、安装包按 CPU 架构拆开、设置里两处「就地弹窗」各自独立成页,
> 顺带把 README 重写了一遍。

### 更新
- **可以接收预发行版了**。以前 RC 只给主动安装的人用 —— 应用内「检查更新」走 GitHub 的
  `releases/latest`,永远只有正式版,所以装了 RC 就收不到任何后续更新。现在「设置 → 接收预发行版」
  默认关闭(只推正式版),打开后手动检查与后台自动检查都会把 Pre-release 一并纳入比较;
  检查更新页上也有同一个开关,切换后立刻按新规则重查。
- **安装包按 CPU 架构拆开**。64 位(`arm64-v8a`)与 32 位(`armeabi-v7a`)各出一个安装包,每台设备
  只用下自己那份 native 库;另有通吃包 `app-universal-*.apk` 与 x86 / x86_64 包(给模拟器)。
  应用内更新按本机实际安装的架构(从 `nativeLibraryDir` 还原)认领对应的那一个,
  不再让人下到装不上的包。
  - 老版本(不带架构信息)会先升到通吃包,之后每次更新就收敛到自己架构的那一个。
  - 服务端 `/version` 用 `apks` 字段按架构分别给出地址与摘要,顶层字段留作通吃包兜底 ——
    这样一次查询就能服务所有架构,CDN 缓存与 GitHub 配额都不会被拆成好几份。

### 设置
- **「AI 密钥」与「课程提醒」各自独立成页**(以前是在设置页里弹窗改)。内容与操作方式不变,
  只是终于有了自己的页面:密钥页管服务商 / 密钥 / 地址 / 模型,提醒页管提前量 / 提醒形态 /
  明日课程预告,底部统一「保存」;原来在弹窗里的「预览实时活动」也挪到了提醒页。
- 提醒页在「明日课程预告」时刻填得不合法时不再允许保存 —— 免得到点不响,却看不出原因。

### 文档
- **README 重写**:补齐徽章、去掉「已知限制」一节,整体语气放软;安装一节改按架构说明该下哪一个包。

### 版本
- 1.0.0-rc1(48) → 1.0.0-rc2(49)。

## 1.0.0-rc1 (2026-09-24)

> 预发布版(RC)。本机编译与构建验证已通过,但「换图标」这件事最依赖真机 —— 各家桌面刷新
> activity-alias 的时机不一样,需要你在设备上点几张确认。确认没问题后再发正式的 1.0.0
> (版本比较是「正式版 > 同号预发布版」,RC 用户会正常收到正式版)。

这一版把「余课」这个身份补全:应用改名、有了自己的角色图标,并且可以把图标
换成你喜欢的那一张、或者让它自己轮着换。

### 应用图标
- **9 张角色图任选**。设置 → 外观 → **应用图标**,九宫格里点一张即换,桌面图标立刻跟着变。
  实现走 `activity-alias`:9 个入口共用同一个名字与同一个 Activity,同一时刻只启用一个
  —— 所以桌面不会多出图标,「换图标」也不必重装。
- **图标轮播**。打开开关后自动在 9 张之间轮换,节奏可选**每次打开应用 / 每小时 / 每天**:
  前一种在进入界面时换(不需要后台任务),后两种由 WorkManager 周期任务换。
- 轮播默认**关闭** —— 开着会盖掉你手动选的那张;想让图标固定的人不该被强制换掉。
- 图标选择与轮播设置**仅存本机**:图标是每台设备自己的事,不参与云端设置同步。
- 说明:桌面图标由系统缓存,切换后一般几秒内更新;个别第三方桌面要久一点(把图标从桌面
  移除再重新添加即可)。

### 外观
- **新增和色「空色」**(#6A96C7):取自角色图那抹浅蓝,压深一档才撑得住白字与选中态。
  强调色从 5 色变 6 色,设置页的色板改为可换行排布(窄屏不再挤爆)。

### 名字
- **应用名改为「余课」**(原「课程表」)。图标、桌面入口、关于页、README 一并对齐;
  Yohaku 的设计取向与包名(`com.kxin.classtable`)不变。

### 版本
- 0.3.6(47) → 1.0.0-rc1(48)。

## 0.3.6 (2026-09-24)

### 雨课堂:同一门课有多条记录时,公告不再互相顶掉
- **公告缓存改为按「雨课堂班级」归属**(原来挂在 App 课程 id 上)。课表里同一门课可能有多条记录
  (周一一条、周五一条,或重复添加/导入),它们会绑定到同一个班级 —— 原来同一条公告会被写上两个
  课程 id,而主键是公告 id,**后写的把先写的顶掉**,结果只有一条记录看得到公告。现在按班级存,
  多条记录共享同一份。
- 顺带修掉一个连带问题:拉取改为**按班级分组**,一个班级只拉一次、只判一次「首次基线」。原来逐条
  绑定循环时,第二个绑定会看到「已有缓存」而把整批历史公告当成新公告 —— 首次同步就推一串通知。
- 数据库 v5 → v6:重建公告表(纯缓存,下次拉取会补回),绑定关系不受影响。
- 说明:同名课程分成多条记录本身是合法的(星期可以在一条记录里多选,也可以分开建);这次修的是
  「多条记录指向同一个班级」时的公告归属与去重。

### 版本
- 0.3.5(46) → 0.3.6(47).

## 0.3.5 (2026-09-24)

### 雨课堂:公告接口定下来了(拉得到公告了)
- **抓到真实端点**。雨课堂的「公告」在服务端是 `topic_type == 9` 的**讨论主题**,所以接口在
  `/v/discussion/v2/` 而不是 `/v2/api/web/`:
  `GET /v/discussion/v2/announcements/?content=&cid={班级id}&limit=&offset=&type=9`。
  之前按常见路径猜的四条候选全都不对 —— 这就是「登录成功却一条公告都拉不到」的原因。
- 客户端改成**只用这一条确认过的端点**(不再四处探测),并支持分页(最多 3 页)。错误约定也补齐:
  这组接口用 `{code, msg, success}`,`/v2/api/web/` 用 `{errcode, errmsg}` —— 现在两套都认,
  否则「无权限」会被当成「没有公告」静默吞掉。
- 解析按真实结构写死:标题 `topic_name`、正文 `content.text`、发布人 `user_info.name`、
  时间 `publish_time`(epoch 毫秒;缺失才退回 `create_time` 的 ISO8601)。
- 「高级 → 公告接口」保留为**接口漂移时的逃生口**:填新路径即时生效,不必等发版。
- 抓包过程只用了「登录 + 读请求 URL」两步,响应体一律由应用自己的 HTTP 客户端获取,
  没有拿浏览器去抓业务数据(按参考文档的约束)。

### 版本
- 0.3.4(45) → 0.3.5(46).

## 0.3.4 (2026-09-24)

### 雨课堂(修一处漏判 + 补排查手段)
- **不再重复推「上课提醒」**:雨课堂自己也会在课前/开课时推上课提醒、开始签到这类消息,而课表本来
  就有课前提醒 —— 现在这类消息**不进新公告通知、也不进课前提醒**(课程详情的时间轴仍照实显示,
  那确实是它发过的内容)。只能按文案识别(接口没有可靠的类型字段),词表刻意保守:认不出的当普通
  公告,宁可漏掉一条提醒,也不误杀老师写的真公告(如「下节课带计算器」)。
- **「一条都没拉到」现在能说清原因**:公告接口候选全部不可用时**抛出并记录**,而不是悄悄返回空列表
  —— 「这门课没有公告」与「接口找错了」终于分得开。设置页的提示也区分开了:还没绑定课程 / 接口没找到
  或服务端报错 / 接口正常但这几门课确实没有公告。
- **日志**:整个拉取过程打到 logcat tag `YuketangSync`(逐班级结果、新公告标题、是否被判为上课提醒、
  原始响应片段);登录页的抓包探针仍在 tag `YuketangProbe`。
- **新增「高级 → 公告接口」**:可以手填接口路径(留空 = 自动探测)。抓到真实端点后填进去、点「立即刷新
  公告」即可验证,不必等发版。
- 顺带:课程对应关系一行改成两行排版,长路径/长说明不再挤在一行里。

### 版本
- 0.3.3(44) → 0.3.4(45).

## 0.3.3 (2026-09-24)

### 雨课堂
- **登录雨课堂**:「设置 → 雨课堂」里内嵌网页登录(扫码或账号密码皆可),登录后把会话 Cookie
  加密存在本机(`EncryptedSharedPreferences`),不上传。`sessionid` 是 HttpOnly,网页脚本读不到,
  只能由原生 `CookieManager` 取。
- **课程公告**:登录后按课名(必要时加教师)把课表里的课自动对应到雨课堂班级;同名课命中多个
  班级时会列出候选让你确认,也可以手动改选。课程详情页新增「公告」时间轴,按时间倒序列出该课
  全部公告。
- **课前提醒带公告**:每节课开始前的提醒里附上该课程**最新一条**公告标题。只读本机缓存、不在
  触发时联网,所以离线与准时都不受影响;标准提醒与「实时活动」两种形态都会带。
- **后台自动拉取**:每小时一次(仅联网时)把公告同步到本机;有新公告时另发一条**独立通知**
  (独立渠道与通知 id,不与课程提醒互相覆盖),点击直达雨课堂设置页。
- **独立开关**:「设置 → 雨课堂」共四个开关 —— 总开关、课前提醒包含最新公告、后台自动拉取、
  新公告提醒;另有「立即刷新公告」可手动拉一次。这些开关属于本机能力开关,不参与设置同步。
  默认全开——登录前它们本来就是空转的(没有会话就没有数据),不必让用户逐个去打开。
- 首次拉取某个班级时把返回的公告整批当**基线**,不会把历史公告当成「新公告」一次性推送。
- 说明:公告接口未收录在参考文档里,当前实现会在若干常见路径间探测并把可用的那条缓存下来
  (集中在 `YuketangClient.ANNOUNCEMENT_PATHS`);Debug 包在登录页注入了抓包探针
  (logcat tag `YuketangProbe`),抓到真实端点后可把它收敛成一条。

### 版本
- 0.3.2(43) → 0.3.3(44).

## 0.3.2 (2026-09-23)

### 课程编辑
- **修逐周勾选歪斜**:周次块原先按文字自适应宽度,「1」和「10」不一样宽,换行位置随之
  参差,整块看起来是歪的;现在统一最小宽度,列能对齐(用最小宽度而不是死宽度,字体放大时
  宁可放宽也不裁掉数字)。
- **修换行后上下贴死**:「星期」与「逐周勾选」两个换行区只给了横向间距,第二行起和上一行
  贴在一起;补上纵向间距。

### 用户问卷
- **用一段时间后邀请一次**。触发不看单一信号,三个条件**同时**满足才开口:装上满 7 天、
  打开过 8 次以上、且已经建过至少一门课——单看任何一条都会误伤(刚装上就问 / 装了没用的
  也被问 / 空课表的人被问「觉得怎么样」)。
- 「装了多久」取系统记录的**首次安装时间**,不用自己记的「第一次打开」:前者跟随安装包,
  升级不清零,所以已经在用的老用户升上来就只差打开次数,不必再白等一周。
- 弹窗只有两个动作:「去填写」(打开 Typeform 表单)与「以后再说」。**两者都不再自动出现**,
  一次打扰为止;表单入口常驻在「设置 → 关于 → 用户问卷」,关了不是死路。
- **打开次数只在真正进界面时数一次**(进程内一次),不放在 `Application.onCreate`——
  推送、小组件、WorkManager 定时任务都会把进程拉起来,那些不算「用户打开过」;
  照那样数,装了不用的用户也会攒够次数被问。
- 一次只弹一个:首次权限引导或更新提示在场时,问卷让位,等它们退场再弹。
- 使用统计(打开次数 / 是否已处理)只存本机,不参与设置同步。

### 版本
- 0.3.1(42) → 0.3.2(43)。

## 0.3.1 (2026-09-23)

0.3.0 系列的正式版:把 rc1–rc13 期间做完的周次、调休、提醒与适配器改动定稿,并加上本条的天气。

### 日视图
- **当前天气**:日期下方新增一块天气卡——大号温度 + 天气现象,下面一行体感 / 湿度 / 风力 /
  空气质量 / 紫外线(接口给多少画多少),右侧是定位到的区县与今日最高最低温。
- 定位走接口按**出口 IP** 自动推断城市/区县:不申请系统定位权限,精度到区县就够;卡片点一下
  可手动刷新,数据半小时自动更新一次,失败保留上一次结果、不弹错。
- 天气图标是自绘的细笔画(晴 / 晴间多云 / 阴 / 雨 / 雷 / 雪 / 雨夹雪 / 雾 / 风),
  按中文天气文案归类、认不出时退回 `weather_icon` 图标码;颜色只取中性档,不占用 accent。
- 卡片放在表头(不随课程列表滚走),今天没课时天气也照常显示。

### 版本
- 0.3.0-rc13(41) → 0.3.1(42),转正式版。

## 0.3.0-rc13 (2026-09-21)

### 周次:范围 + 单双周终于算对了
- **修判定**:单/双周以前完全忽略 `weekStart..weekEnd`,只按 `week % 2` 判 —— 「第 3-19 的双周」
  在第 2、20 周也会显示。现在范围生效,这门课只落在第 4、6、…、18 周。
- **不再压扁**:导入侧(适配器 / AI 识别 / 手动表格)以前把离散周次压成 `min..max`
  (`3,5,7-9,11` → `3..11`);现在逐条精确 —— 能整体表达成每周/单周/双周就用档位,否则原样保留集合。
- **「自定义」可逐周勾选**:课程编辑页的「自定义」从「起始周 / 结束周」两个输入框,换成一排周次块
  (1..学期周数,点选即切换),配「全选 / 清空」;一个都没勾时保存按钮禁用。
- **单 / 双周可限定范围**:选中后填起止周(结束周留空 = 到学期末),于是「3-19 双周」可以直接填出来。
- **文案收敛**:周次显示统一走 `WeekSpec.text`,能直接写出「第3-19周(双周)」「第1-3,5,7-9周」;
  手动导入的周次列也认得 `3-19双周` 这种写法(以前只会把它当 `3-19`)。
- 存储与同步:课程新增 `weeks`(CSV)字段;本地库 v3 → v4 只加列不回填,远端 Firestore 同名字段。
- 旧数据不迁移:老的「单/双周」(当时写死 1-16)会截断到 1-16 周,重新导入即可。

### 版本
- 0.3.0-rc12(40) → 0.3.0-rc13(41)。

## 0.3.0-rc12 (2026-09-21)

### 周视图
- 撤掉上一版加的「每个节次一条横向刻度线」:周视图回到**不画线**的样子,只保留「打开落回
  当前周」与「『正在上』只在真实当前周的今天标出」这两项。

### 小组件
- **修掉卡片(横条)偏窄**:桌面小组件设置页的内容列多补了一层 `screenPadding`,而
  `SettingsSection` 自带这一层,于是每张卡片左右各缩 40dp、比设置页窄一截。去掉多余那层后,
  卡片宽度与设置页一致。

### 版本
- 0.3.0-rc11(39) → 0.3.0-rc12(40)。

## 0.3.0-rc11 (2026-09-21)

### 适配器:基线回到 rc2,并从上游重拉
- 基线重置回 `0.3.0-rc2`,再从上游镜像(gitee)整份重新拉取适配数据。差异只在长江大学
  `yu.js`:恢复上游版本(含 12:00 午间课与「节次 → 时间段」重排表),去掉本地那几处补丁。
- `yaml2json` / `build-netlify` / `verify-warehouse` 全部通过(218 校 / 242 适配器)。

### 周视图
- **打开即当前周**:翻页状态是可保存的,进程重建后会停在上一回合翻到的那一周;现在进入
  周视图(以及跨到新的一周)都会拨回**真实当前周**。
- **「正在上」只在今天标出**:补上真实当前周判断 —— 以前翻到别的周时,只要时刻对得上,
  那一列也会被标成「正在上」。
- **左侧节次列加行刻度**:每个节次画一条横向细线(与左侧「节号 + 起始时间」、课程块共用
  同一行高),左侧那一列不再是一串没有参照的文字。

### 作息时间
- 「添加一节」不再只能往末尾加:点任意课间的「加一节」即可在中间插入一节。
- 课间长短改为明确的「－ / ＋」(1 分钟一档),不再只靠右下角那个不显眼的手柄。

### 调休课表
- 自动匹配出来的补课建议现在可以逐条**修改「补哪天的课」**(点「修改」重选原课程日期),
  不必因为匹配不准就整条放弃。

### 提醒与小组件
- **默认提醒形态为「实时活动」**;明日课程预告默认**开启**。
- 小组件设置页去掉多留的底部空白(它是二级页,不该给悬浮导航预留高度)。

### 版本
- 0.3.0-rc2(30) → 0.3.0-rc11(39)。`rc3`–`rc10` 的编号已被此前的标签占用,故顺延到 rc11。

## 0.3.0-rc2 (2026-09-20)

### 界面
- **悬浮导航变细**:栏体竖向收薄(单项内边距 10→7dp、栏内 4→3dp、上下浮动余量 12→8dp),
  高度约 53 → 44dp;内容预留量 80 → 64dp 同步收紧。
- **周视图适配导航栏**:网格改为排在悬浮导航**之上**。以前让它铺到屏幕底部、从栏下穿过,
  结果是最后一两节被导航栏压住、还得滚动才看得见 —— 对课表来说这是错的。现在七行正好铺满
  可见区域,末尾那段预留滚动也一并去掉。
- **小组件设置页排版**:「添加到桌面」原来把「1×1 · 下一节的名称 / 时间 / 地点」这类说明塞进
  单行设置的**值**槽位,标题被压、说明被截(排版不协调的来源);改成标题 + 说明两行。
  「课表最多显示」的片从五个减到四个(2/4/6/8),窄屏不再挤爆。

### 版本
- 0.3.0-rc1(29) → 0.3.0-rc2(30)。

## 0.3.0-rc1 (2026-09-20)

> 预发布版:本机编译与逻辑用例通过,但还没有在你的设备上跑过。确认没问题后再发正式的 0.3.0
> (RC 用户会正常收到正式版,见下面「发布流程」)。

### 修复:「当前时间线」位置不对
- 根因是**时区**:原来用「时间戳 / 60000 % 1440」算「现在几点几分」,那其实是 **UTC** 的分钟数,
  东八区整整差 8 小时 —— 线要么画在错误的那一行上,要么落不到任何一节课里、干脆不画。
- 新增 `Schedule.minuteOfDay(millis)`(按系统时区换算);周视图的当前时间线、日视图的
  「正在上课 / 距下一节还有多久」都改用它。已用脚本验证:东八区 14:30 旧公式得 06:30、新公式得 14:30。

### 修复:批量删除按钮与悬浮导航栏重合
- 课程页多选后的操作条在根标签页底部没有为悬浮导航留位置,「删除 N 门」被导航栏压住。
  现在把它抬到导航栏之上。

### 发布流程
- 版本比较器现在能区分预发布版:`0.3.0-rc1` 与 `0.3.0` 此前会被当成同一个版本,发过 RC 之后
  RC 用户就永远收不到正式版了。规则改为「正式版 > 同号预发布版;同号预发布之间比编号」,
  并补了 8 条用例验证。

### 版本
- 0.2.7(28) → 0.3.0-rc1(29)。

## 0.2.7 (2026-09-20)

### 修复:长江大学「一节课莫名其妙变长」
- 根因是**合并连续节次只看节次号**:删掉 12:00 那一节之后,上午最后一节(10:05–11:40)与
  下午第一节(14:00–15:35)的编号恰好接上了(2 和 3),于是被并成一节 10:05–15:35。
- 合并现在还要看**时间**:两节之间只隔一个课间才算连续,隔了午休/晚休(按超过 60 分钟算)不合并。
  已用脚本逐条验证(跨午休的 2+3 不再合并,正常的 1+2 / 3+4 / 4+8 / 5+6 仍合并)。

### 修复:节假日服务不可用
- 之前只挂一个数据源,它一抽风整个「获取节假日与调休」就不可用。现在按顺序尝试三个来源
  (完整接口 + 开源节假日数据的两个 CDN),任一可用即可;失败时把试过哪些来源与原因一并说明。
- 两种返回格式都支持(timor 的 `holiday/wage`、holiday-cn 的 `days/isOffDay`),按字段名自动分辨。
  已实测三个源 2026 年数据均可取,字段与解析一致(全年 39 条、含 6 个补班日)。

### 界面
- **周视图左侧时间条看不清**:起始时间原来是 8sp + 最淡那一档灰(neutral5),基本读不出来。
  现在 10sp + 正常墨色,节号也提到 10sp 并加深,留白列从 28dp 加宽到 36dp 容得下「08:00」。
- **「今天」那一列中间的横条**:那是「当前时间线」,但一条孤零零的线没人知道是什么。现在带一个
  「现在」标签(压纸面底色,叠在课程块上也读得出)。

### 版本
- 0.2.6(27) → 0.2.7(28)。

## 0.2.6 (2026-09-20)

### 调休课表:自动获取与自动匹配
- 调休页新增「获取节假日与调休」:填年份,从公开节假日接口取全年安排,按连续放假区间分组成
  假期,并给每个补班日**自动匹配**它最可能替代的那个教学日期(原课程日期),生成预览。
- **预览不写数据**:逐条勾选「已选 / 忽略」,点「采用所选日期」才并进调休表;学期之外的日期
  自动过滤(没设开学日时无从判断,全部保留)。
- 语义对齐参考实现并修正了一处:**补课日不追链** —— 原课程日期自身落在被停课的假期里时
  (如 10/11 补 10/2 的课),补课日仍然是「上课」,不会被判成停课。

### 调休接进提醒与小组件
- 提醒排程改为按「调休后的教学日」取课:停课日不排提醒,补课日按原课程日期那天的课排;
  「明日预告」同理。
- 提醒的**排程签名**补上调休字段(并升签名版本,让已装机重排一次)—— 否则改了调休而签名没变,
  排程会直接跳过,提醒还停在旧安排上。
- 三个小组件都按调休显示:停课日明确说「今天 / 明天停课(调休)」。

### 桌面小组件:内容 / 样式 / 种类 + 自定义
- **新增「明日课程」小组件**(2×2):今天就能看到明天要上什么,不必等到第二天。
- **小组件设置页**(设置 → 桌面 → 桌面小组件):原先只有一个「添加到桌面」弹窗,现在是一个
  独立页面 —— 三款小组件一键添加,加上显示内容(时刻 / 节次 / 地点 / 教师)、课表最多显示
  几门、行距(标准 / 紧凑)、底色(纸面 / 深色 / 轻透)。
- 改完立即刷新桌面上的小组件;强调色不再写死在小组件里,跟随「外观」里的设置。
- 内容更丰富:今日 / 明日课表的表头带「周几 · 第几周」,超出设定门数时提示「还有 N 门」。

### 版本
- 0.2.5(26) → 0.2.6(27)。

## 0.2.5 (2026-09-20)

### 新增:调休课表
- 设置 → 课表 → **调休课表**:给某一天安排「停课」或「补课」。补课时选择**原课程日期**,
  那天就按原课程日期那一天的课表上课(第几周 + 星期几)。
- 关键语义:**课程不会被搬动**。调休改变的是「这一天该取哪一天的课」,所以课程仍然属于它
  本来的星期与节次,随时可以撤销;补课日不做二次调休传递(避免「补的课又被补走」)。
- 周视图表头给调休日标「休 / 补」,那一列也按调休后的课表绘制;今日课表顶部标出
  「调休 · 今天停课」/「调休 · 今天补 x/x 的课」,并在停课时明确显示「今天停课(调休)」。
- 列表页可新增 / 修改 / 删除,条目按日期排序。

### 新增:课程多选与批量删除
- 课程页顶栏「选择」进入多选,行首出现勾选圈,底部换成「全选」+「删除 N 门」;
  批量删除只触发一轮小组件刷新与提醒重排(逐条删会触发 N 轮)。

### 改进:作息编辑界面
- 每个时刻都变成**可点的时间片**,点哪个就改哪个;不再是「点整张卡片改开始时间、
  拖右下角改时长」这种要靠提示才猜得出来的操作。每一节都有「删除」,底部的
  「＋ 添加一节」改成真正的按钮。
- 新增次级按钮样式(浮起面 + 细边框 + accent 文字):并列的次要动作不该和实底主按钮抢注意力。

### 适配器
- 长江大学:去掉 12:00 那一节(学校实际没有这一节),其后的时间段顺次上移,
  落在该节的课程不再导入。

### 版本
- 0.2.4(25) → 0.2.5(26)。

## 0.2.4 (2026-09-20)

### 作息与课程的时间
- **适配器带了作息就强制覆盖当前作息**:确认页不再提供「要不要应用」的勾选 —— 脚本给的作息就是
  这所学校的作息,直接覆盖;同时把「覆盖了什么」(作息几节、学期起止)写在明面上,不静默。
- **课程的时间点钉死**:导入 / 新建课程时,按当时的作息把「第几节」换算成具体时刻存进课程本身,
  之后**再改作息、或再导入别的学校,都不会移动已有课程的时间** —— 只有你自己编辑那门课才会变。
  课程列表仍按「第 N-M 节」标注,只有真正没有节次的自定时间课程才写「自定义」。
- **修掉「课程时间对、但跟左边节次对不上」**:周视图左侧的节次列此前只在**奇数行**标开始时间
  (按「2 小节 = 1 大节」的默认表),作息本身就是大节的学校(长江大学 8 个 95 分钟的节)会有
  一半行没有时间,第 2 节的课紧挨着的标签是第 1 节的 08:00。现在每一节都标出开始时间。
- **日视图 / 桌面小组件的「正在上」「下节课」改为按课程自身时间判断**,不再用「2 小节 = 1 大节」
  配对推算 —— 大节作息下前者会把上一节标成正在上,后者会把已经上过的课当成下节课。

### 版本
- 0.2.3(24) → 0.2.4(25)。

## 0.2.3 (2026-09-20)

### 修复:导入后作息时间没生效
- **根因**:适配脚本回传的节次时间可能是 `HH:MM:SS`(强智 / 正方系教务接口普遍如此,例如
  开始时间字段的原始值形如 `08:30:00`),而应用侧的时间解析要求恰好是 `HH:MM` —— 带秒的条目会被
  整条丢弃,于是**课程正常导入、作息却停留在默认值**,确认页还会提示「脚本未提供作息时间」。
- 时间解析改为兼容 `HH:MM:SS`(秒直接丢弃)与 `8:00` 这类写法。
- 解析失败不再静默吞掉:记入日志,并明确提示「脚本返回的作息时间无法识别,已保留原作息」。
- 此前遇到「课程导进来了、作息没变」的话,升级后**重新导入一次**即可;已导入的课程不受影响。
- **确认页的课程时间改用脚本识别到的作息来算**:此前它拿 App 内置的默认作息表算,而上方那行
  作息预览用的是脚本那张表 —— 学校作息是大节(如长江大学 95 分钟一节)时,同一门「第 1-2 节」
  会显示成两个不同的时间,看起来就是「课程没按照作息时间」。

### 界面
- **悬浮底部导航**:之前只是「贴底居中的一条灰栏」。现在补齐悬浮该有的三件事 ——
  四边不贴边、用投影把层级画出来、**内容从栏下穿过**;形状改为胶囊。
  最后这一条尤其明显:上一版给内容留了底部空间,内容在栏顶就结束了,滑动时课程永远碰不到
  导航栏;现在内容一直铺到系统手势条、导航压在它上面,列表滚动时从栏下经过,而末尾在滚动
  内容里预留栏体高度,最后一屏不会被永久盖住。
- **去掉灰框子**:卡片 / 弹窗 / 设置分组 / chip 的底色此前比纸面更暗,浅色下看着就是一个个
  「深色框」。新增浮起面与细边框两个色板 token(浅色下容器比纸面更白、深色下更亮),
  层级改由「更亮 + 投影」表达,细边框只做收边。
- **作息时间改为时间线编辑器**:节与课间交替成块,方块高度按分钟成比例,一眼看得出哪节长、
  哪个课间久;**拖右下角调时长**(1 分钟一档),**点方块选开始时刻**,按上午 / 下午 / 晚上分段。
  课间由相邻两节自动得出,改一节后面整体顺移,不会再出现时间重叠或乱序。
- 新增自绘时间选择器(时 / 分步进 + ±5 / ±45 分钟快捷 + 精确输入),取代逐格手敲 `HH:MM`;
  每一节旁边直接标出时长与课间。

### 版本
- 0.2.2(23) → 0.2.3(24)。

## 0.2.2 (2026-09-19)

### 更新下载重写
- **找到「下载不成功」的原因**:代理返回的安装包带 `Content-Encoding: gzip` 且没有 `Content-Length`,
  而旧实现拿 `contentLengthLong` 当进度基准、还用它做完整性判断 —— 于是进度永远停在 0%,
  或者长度对不上直接判成「安装包不完整」。
- 进度改为按**已写入字节数**计算,总长取服务端 `/version` 新增的 `apkSize`,不再依赖响应头。
- 请求显式声明 `Accept-Encoding: identity`,避免压缩响应带来的长度错位。
- **分段 + 断点续传 + 重试**:单次请求 4MB(绕开代理/边缘的单响应上限 —— 超出会被截断成坏包),
  每段最多重试 4 次;弱网断开后从已下载的位置继续。
- **SHA-256 + 包内版本号双重校验**:服务端从 Release 资产摘要透出 `apkSha256`,安装前再核对
  包内 `versionName`;宁可报错让用户重试,也不把坏包装进系统安装器。
- 下载挂在数据层(不在页面 ViewModel 里):切页、返回都不会中断,重新进入还能看到进度;
  失败可「重试下载」。
- 服务端 `/apk` 支持 `Range` 透传与 206 响应,整包超过单响应上限时明确报错而不是返回半截文件;
  JSON 响应补 `charset=utf-8`(避免更新说明出现乱码)。
- 安装流程:先确认文件可解析、再引导「安装未知应用」权限;`ACTION_VIEW` 失败时退回
  `ACTION_INSTALL_PACKAGE`,并把失败原因直接告诉用户。

### 界面
- **底部导航改为悬浮栏**:从「周视图 / 日视图 / 课程」三个页面各自的布局里抽出来,统一由 MainActivity
  承载,浮在内容之上(底部居中 + 导航栏内边距),宽度只包住四个标签 —— 不再是一整条贴边的长条;
  选中态由「accent 实底胶囊」改为「accent 文字 + 小圆点」。二级页不再显示导航,四个根页面
  的内容在导航上方结束,不会被遮住。
- **设置页重排**:按语义分组(外观 / 课表 / 导入与识别 / 提醒 / 桌面 / 账号与数据 / 关于),
  每组一张纸面卡片配小标题,组间距与行间距分开;设置成为根标签页后,顶栏不再显示返回箭头。
- **检查更新页渲染 Markdown**:新增 `YohakuMarkdown`(标题 / 列表 / 分隔线 / 引用 / 加粗 / 代码 / 链接),
  发布说明按块排版,不再原样显示 `###`、`-`、`**` 这些记号;同时显示安装包大小与下载进度(MB)。

### 版本
- 0.2.1(22) → 0.2.2(23)。

## 0.2.1 (2026-09-19)

**状态栏胶囊(实时活动)适配修复** —— 荣耀灵动胶囊、小米超级岛、OPPO 实况通知这类国产胶囊此前在
本应用上都不会出现,原因是通知不满足 Android 16 Live Updates 规范里的一条硬条件。

### 修复
- **补上 `POST_PROMOTED_NOTIFICATIONS` 声明**(清单里的非运行时权限):它是「系统是否允许把通知
  提升为实时活动」的前提,缺了它任何胶囊机型都不会提升本应用的通知。这一条是本次最关键的变化。
- **实时活动改用平台 `Notification.Builder`**:`ProgressStyle`(API 36 的进度轨 + 进度点)、
  `setShortCriticalText`、提升请求都只在平台 Builder 上可达,compat Builder 拿不到。
- **主动请求提升**:按规范写入 `android.requestPromotedOngoing` 与 `android.shortCriticalText`,
  并在方法存在的版本上补一次 `setShortCriticalText` 调用(签名有 String / CharSequence 两种,都试)。
- **胶囊短文案改为纯文本且 ≤7 字**(如「10分钟」):状态栏胶囊宽 96dp,超长会退化成只显示图标;
  带 span 的文案在部分机型上会被渲染器丢弃。
- **上课中改用 `CATEGORY_PROGRESS` + 进度轨**,课前用 `CATEGORY_EVENT`;固定 `COLOR_DEFAULT`,
  不使用自定义颜色(部分系统会把自定义色通知按普通通知渲染)。
- **划掉通知 = 取消本节提醒**(delete intent 复用静音动作):规范要求被用户划掉的实时活动不得重新发布。
- **前台服务类型 dataSync → specialUse**:实时活动是「持续展示进行中状态」,在 Android 15+ 上
  dataSync 受每日时长上限约束;清单同时补 `FOREGROUND_SERVICE_SPECIAL_USE` 权限与服务子类型说明。
- 新增小米「超级岛」参数:`miui.focus.param`(`param_v2`:交互能力 / 摘要态 / 焦点通知数据)+
  `miui.focus.pics`,按官方《超级岛开发指南》客户端接入方式实现;**先查设备再补参数**
  (是否小米系 / `persist.sys.feature.island` / `notification_focus_protocol` ≥ 3 / `canShowFocus`),
  任一不满足就不动通知;参数内 `filterWhenNoPermission=false`,权限被关时退化为普通通知。
- **诊断日志**:构建实时活动时打印 `promotable` / `requested` / `canPostPromoted` / 胶囊文案 /
  小米岛状态,装到真机上直接看日志就能定位胶囊为什么不出现。

### 设置
- 「提醒可靠性」页新增「**实时活动胶囊**」一项(API 36+):显示系统是否允许提升,点击直达系统
  「实时活动」设置页(没有该页面时退回应用通知设置)。

### 版本
- 0.2.0(21) → 0.2.1(22)。

## 0.2.0 (2026-09-19)

界面与提醒的一次整体收束,也是文档的一次重写。

### 文档
- README 重写为**产品介绍**:面向使用者讲清楚能做什么、怎么装、隐私边界与常见问题,不再堆技术细节。
- 原有技术内容整理进 `docs/`:新增 [架构与技术栈](docs/architecture.md)、[教务导入系统](docs/import-system.md)、[后端部署与配置](docs/backend-setup.md);README 末尾只保留一张文档索引表。

### 本轮功能汇总(0.1.18 → 0.1.19 已完成,详见下文)
- 全部弹窗、日期选择器、芯片等改为项目自绘,不再沿用 Material 默认外观。
- 周视图一屏看全一周七天,左右滑动翻周。
- 导入入口收拢到周视图右上角「添加」(教务导入 / 手动添加 / 手动表格导入 / AI 图片导入)。
- 自动检查更新:可开关、可忽略某版本、后台每天一次、发现新版发通知。
- 通知模块重写:排程签名 + 已排台账、8 天滚动窗口自续期、实时活动、通知操作按钮、明日课程预告。
- 版本 0.1.19(20) → 0.2.0(21)。

## 0.1.19 (2026-09-19)

**通知模块重写**。排程核心换成 `notify/ReminderPlanner.kt`(旧的 `NotificationScheduler` 已删除)。

### 排程
- **排程签名**:课程(含作息/学期/通知设置)的指纹,只有指纹变化或强制时才真正重排。此前「课程或设置任意一次发射」都会全量取消 + 重排。
- **已排台账**:把真正排出去的 `requestCode|action|课程` 记在本地,取消时精确撤销;删课不再盲扫 0..120 天。
- **滚动窗口 + 自续期**:窗口 8 天,并额外排一个**次日 00:05 的刷新闹钟**,由接收器强制重排 —— App 长期不打开也能把窗口往前滚。
- **实时活动重试补发**:实时活动的触发点会排 **+0/+1/+3/+5 分钟**四次,系统丢闹钟或延迟送达时后一次仍能把状态补上。
- **精确闹钟降级**:未授权时退到窗口闹钟(±1 分钟),并捕获 SecurityException;授权状态变化时由 Boot 接收器重新排成精确闹钟。

### 实时活动(课前 → 上课中 → 下课)
- **不再限于 Android 16**:任何支持的版本都能常驻;API 36+ 额外注入 promoted 胶囊(状态栏小字),用反射 + 兜底,失败只记日志。
- **按分钟边界对齐刷新**(不用 chronometer,它会被系统换掉胶囊文案),下课 1 分钟后自动收掉。
- **payload 持久化 + 恢复**:进程被杀后服务按 START_STICKY 重启仍能接着显示同一节课。
- **通知操作按钮**「取消本节课提醒」:按「课程:日期」静音到下课为止,重试闹钟与重启都不会再拉起。

### 可达性与健壮性
- 通知可达性检查现在**包含渠道 importance**(渠道被关掉时不再以为「发成功」了);`notify` 统一捕获 SecurityException。
- 提醒触发走 **5 秒短时唤醒锁**,保证系统再次休眠前读完数据并发出通知(新增 `WAKE_LOCK` 权限)。
- **明日课程预告**(可开关 + 自定义时刻):前一天晚上提醒「明天有 N 门课 · 第一节几点在哪」。
- 普通提醒通知 **id 按「课程 + 日期」区分**,不同天的提醒不会互相覆盖;FCM 推送与本地闹钟同 id 去重。
- Boot 接收器补上 **`LOCKED_BOOT_COMPLETED`** 与 **精确闹钟权限变化** 两个 action。

### 设置
- 「课程提醒」弹窗新增:**提醒形态**(标准提醒 / 实时活动,附说明)、**明日课程预告**(开关 + 时刻校验)、**预览实时活动**(用一节假课程把常驻通知拉起来看效果,不写数据)。
- 设置行摘要会显示当前形态(如「课前 10 分钟 · 实时活动」)。

### 说明
- 未做(留待后续):跨相邻课程合并成一次连续实时活动、勿扰自动规则(AutomaticZenRule,需要额外的策略访问权限引导)。

## 0.1.18 (2026-09-19)

### 组件去 Material 化
- **新增 `YohakuDialog`**(foundation `Dialog` + 纸面自绘:neutral2 底、1dp neutral5 细边框、radiusSheet 12dp、无阴影、项目风格的纯文字动作),**8 处 `AlertDialog` 全部迁入**:启动更新提示、周视图课程速览与「添加」弹层、设置页 AI 密钥 / 课程提醒 / 桌面小组件 / 自动检查更新、导入页选适配器与适配器详情。
- **主题层补齐映射(根因)**:`Theme.kt` 现在把 `Typography` 映射到 Yohaku 字阶(此前是空 `Typography()`,对话框标题实际按 Material 的 24sp Roboto 渲染)、`Shapes` 收敛到 4/6/8/12dp(此前最大 28dp),并把 Material 组件会消费的颜色槽(`surfaceContainer*` / `onSurfaceVariant` / `secondary` 等)全部指到中性色 —— **弹窗底色那层紫调 baseline 消失了**。
- **新增 `YohakuIndication`**:按下时整块淡淡压深,替代 Material 涟漪(此前全项目每个 `clickable` 都带 M3 涟漪)。
- `YohakuChip` 改为 `Box + selectable` 自绘(去掉 Surface 自带涟漪,同时让读屏能报出「已选中」);`YohakuButton` 显式覆盖内边距、最小高度与 elevation。
- **新增 `YohakuDatePicker`** 自绘月历,替换学期页的 Material `DatePicker` + 两个 `TextButton`(全项目最「Material」的一面)。

### 自动检查更新
- 设置页新增「自动检查更新」开关(默认开启),行内显示是否已忽略某版本。
- 新增后台任务(WorkManager 每天一次、仅联网时执行)检查版本;发现新版本发一条通知(新渠道「应用更新」),点击直达检查更新页 —— **只提示,不自动下载**。
- 启动提示新增「忽略此版本」:同一版本不再主动打扰(此前 24h 后仍会再弹)。
- 检查节流与开关收拢进设置(原先记在 `filesDir/update/meta.json`)。

### 周视图滑动翻周
- 周视图改为**按周分页**:左右滑动即翻周,顶部 `‹` `›` 按钮移除;「今天」跳回真实当前周。当前时间线与今天列底色只在真实当前周出现。

### 未完成(下一版继续)
- **通知模块重写**(排程签名、滚动窗口自续期、实时活动重试补发、通知操作按钮、明日课程预告)。
- 适配脚本的原生对话框(`ImportBridge` 内 3 处 `android.app.AlertDialog`)改为 Compose 纸面弹窗。

## 0.1.17 (2026-09-19)

### 周视图重做:一屏看全一周七天
- 原来的「一屏一天 + 左右翻页」改为 **7 列网格**(周一~周日):横向七列,纵向按节次等高等分,**七天与全部节次始终同屏**。
- 行高不再写死 88dp,而是 **可用高度 ÷ 节数** 动态计算(下限 38dp);小屏或节次特别多时整格纵向滚动,不会挤塌。
- 左侧留白列从 76dp 收到 **28dp**,只放节号与每个大节的起始时间,把宽度让给七列;表头带星期与日号,今天用 accent 标出,并给今天整列一层极淡底色做方位提示。
- 课程块仍是绝对定位横穿节次区间(自定义时间课按分钟比例),但补上了 **并排分栏**:同一时段多门课按车道平分列宽,不再互相覆盖。
- 新增 **当前时间线**:只在今天那一列画一条 accent 横线,按分钟定位,每分钟刷新。
- 课程块改为「课程名 + 教室」,够高才显示教室;窄格子下不再塞时间(时间由行位置给出)。

### 课程配色
- 新增 `design/CoursePalette.kt`:每门课按**课名**派生一个和色色相(梅/縹/若竹/朽葉/蘇芳/藤/浅葱/苗),同名课永远同色。
- 只做**极淡的纸面色差**(浅色/深色两套),不引入分类色贴纸感;accent 仍然只表示「此刻正在上」。颜色不承载语义(课程名始终在),灰度与色盲下信息不丢。
- 应用到周视图块、日视图卡片、课程列表左侧色标、课程详情顶部色块。

### 课程页与详情重写(并修正节次时间)
- **修掉一个真 bug**:`Schedule.DEFAULT_PERIODS` 原本是「只有开始时间」的旧格式序列化产物,每两节中的第二节被拉长 —— 第2节 `08:50–10:10`、**第4节 `11:00–14:00`(横跨午饭)**、第8节 `17:00–19:00`,整条时间线没有任何课间。现在换成每节 50 分钟、含 10 分钟课间与午/晚餐间隔的正规作息(08:00/09:00/10:10/11:10 · 14:00/15:00/16:10/17:10 · 19:00/20:00/21:00/22:00)。**节次起点尽量保持不变**,所以上课提醒不受影响;而详情、列表、日视图、小组件、当前课判定的显示一起修正。
- 存量的旧默认值会被识别为「没设置过」自动换用新表(学校导入保存过的真实作息不动)。
- **课程列表重写**:按星期分组(周一~周日,只列有课的天),每行左侧课程色标、右侧给出「第 3-4 节」与「10:10–12:00」两行信息;空态指向周视图右上角「添加」。
- **课程详情重写**:节次与时刻**分开成两行**(不再混成「3-4 节 · 10:10–14:00」这种读不通的写法),顶部加课程色块。

### 导入入口调整
- 底部导航去掉「导入」项(现为 **周视图 / 日视图 / 课程 / 设置** 四项)。
- 周视图右上角「添加」改为弹层,收拢四种方式:**教务导入**(3 步,学校适配器)、**手动添加**单门课、**手动表格导入**、**AI 图片导入**。
- 教务导入页第一步不再单列「手动表格导入 / AI 图片导入」两个入口(已移到上面的弹层),该页专注 3 步流程。设置页的「教务导入」入口保留作为备用。

## 0.1.16 (2026-09-19)

**「点执行导入就弹已取消导入」的真正根因:调用注入方法时把 `this` 丢了(`apply(null, ...)`)。**

0.1.15 的诊断只说对了一半 —— 对象确实被判成「非注入」,但**触发条件在垫片自己的调用方式**,与 SSO 页面重写文档无关。

### 根因
垫片里所有 Promise 化的方法都经过 `callNative`,而它是这样调的:

```js
AndroidBridgeNative[method].apply(null, args.concat([id]));
```

WebView 的注入方法要求 `this` **就是那个注入对象本身**。`apply(null, ...)` 在非严格模式下会把 `this` 换成全局对象,Chromium 检查后直接抛:

```
Error invoking showSingleSelection: Java bridge method can't be invoked on a non-injected object
```

而 `showToast` 是**直接调用**(`AndroidBridgeNative.showToast(...)`),`this` 天然正确,所以它一直正常 —— 这正是长期误导排查的地方(看起来像「桥是好的」)。凡是走 `callNative` 的 `showAlert` / `showPrompt` / `showSingleSelection` / `saveImportedCourses` / `savePresetTimeSlots` / `saveCourseConfig` 全部失败,垫片又把异常吞成 `resolve(null)`,适配器把 `null` 当成「用户取消」,于是弹出「已取消导入」。官方桥只有一个 `postMessage` 直调入口、从不使用 `apply`,所以同一个适配器在官方 App 里是好的。

### 修复
- `callNative` 改为 `AndroidBridgeNative[method].apply(AndroidBridgeNative, fullArgs)`。
- 保留一个**兜底通道**:万一直连仍然失败,垫片改用 `console.log('__SHIGUANG_BRIDGE__' + JSON)` 把调用送到原生 —— 网页控制台与 `evaluateJavascript` 都不依赖注入对象。原生在 `onConsoleMessage` 里解析分发,回填仍走 `window.__resolve`。
- 0.1.15 的「每次页面加载重新注册桥对象」保留(无害且对个别机型有用)。
- 版本号 0.1.15(16) → 0.1.16(17)。

### 验证
- `verify-bridge` 新增 2 项回归:①调用注入方法时 `this` 必须是注入对象本身(**直接钉死本次根因**);②直连失败时能经 console 通道送达并回填。

## 0.1.15 (2026-09-19)

**「点执行导入就弹已取消导入」的真正根因,已在真机上抓到了。**

在 0.1.14(带埋点的诊断版)上、用真实登录态跑长江大学适配器,拿到这段话:

```
[桥] showSingleSelection 调用失败: Error invoking showSingleSelection:
    Java bridge method can't be invoked on a non-injected object
[桥] showAlert 调用失败: Error invoking showAlert:
    Java bridge method can't be invoked on a non-injected object
```

### 根因
教务系统普遍走 CAS / 深信服 aTrust 这类 SSO。这类页面会在**同一个 JS 上下文里重写文档**(document.write / 前端路由跳转),WebView 随之把先前注入的 Java 对象标记为「非注入对象」,此后该文档上的**每一次桥调用都会抛异常**。

我们原来只在创建 WebView 时调用一次 `addJavascriptInterface`,而垫片虽然幂等、却是在页面早期注入的;SSO 跳转过后垫片依旧在跑,只是它拿到的桥对象已经失效。垫片当时又把异常**静默吞成 `null`**,适配器把 `null` 理解成「用户取消」,于是弹出「已取消导入」——真实错误被完全掩盖。

### 修复
- **每次页面开始/加载完成都重新注册桥对象**(`addJavascriptInterface`)再注入垫片;「执行导入」注入脚本前也刷新一次。接口可重复注册,这会为当前上下文换上新的可用对象,而垫片是实时读取它的,因此旧垫片立即恢复可用。
- **垫片遇到桥调用失败改为 reject,不再回填 `null`**(官方在原生处理失败时同样是 reject)。这样同类故障会显示成「导入失败:<真实原因>」,而不是骗人的「已取消导入」。
- 版本号 0.1.14(15) → 0.1.15(16)。

### 说明
- 仍保留 0.1.14 引入的 WebView 调试开关与桥调用埋点(便于在真机上复核);确认无误后随下一版移除。

## 0.1.14 (2026-09-19)

**诊断版(临时)。** 0.1.13 的埋点已经能记录桥的每一次调用与回填值,但「点执行导入就提示已取消导入」需要把**真实设备上那一刻**的调用序列拿到手,因此这一版额外打开 WebView 调试开关:可以通过 `adb` + CDP 在真机页面里直接执行脚本、核对桥的行为,**不必登录教务系统**。

### 变更
- `configureImportWebView` 中开启 `WebView.setWebContentsDebuggingEnabled(true)`(仅导入页存活期间)。
- 其余与 0.1.13 相同:桥调用轨迹(`tag: ImportBridge`)、网页控制台(`tag: ImportWebView`)。

### 说明
- 这是定位问题用的版本,**下一版会移除调试开关**。
- 本地复现(真实垫片 + 长江大学 `yu.js` + 模拟教务返回)显示:正常路径下 `showSingleSelection` 能正确拿到学期列表并回填序号,不会走到「已取消导入」;因此需要在真机上核对实际发生的调用。

## 0.1.13 (2026-09-19)

这一版只加**诊断埋点**,用来一次定位「点执行导入就提示已取消导入」:适配器只用 showToast 反馈结果,出问题时「哪一次桥调用、回填了什么」才是关键证据。

### 变更
- 桥的每次调用都打到 logcat(`tag: ImportBridge`):方法名 + 关键参数(`showAlert` 标题/按钮、`showPrompt` 默认值/校验函数名、`showSingleSelection` 选项数与默认项、`save*` 的字符数)、以及**回填给脚本的值**(`→ resolve(id) = ...`)。
- 弹窗创建失败、回填注入失败都会记录原因;`showSingleSelection` 选项为空时会明确记下「回填 null(适配器多半会判成用户取消)」。
- 垫片侧:`callNative` 现在把**调用失败**与**60 秒超时**用 `console.error('[桥] …')` 报出来(以前被静默吞成 `null`,适配器只会说「已取消导入」)。
- 网页控制台输出改用 `Log.i`(tag: `ImportWebView`),避免被部分 ROM 过滤掉 `Log.d`。
- 日志取值:`adb logcat -s ImportBridge:I ImportWebView:I`

## 0.1.12 (2026-09-19)

**「点执行导入就提示已取消导入」的真正根因,找到了。**

### 修复
- **适配脚本被 IIFE 包裹,导致它的顶层声明进不了全局作用域**:我们一直把脚本包成 `(function(){ try{...}catch(e){...} })();` 注入(本意是让同步异常能上报)。但适配器普遍用**裸函数声明**写 `showPrompt` 的校验函数(全库 72 个,例如东北大学 `function validateYearInput(input){...}`),而校验是按名字在**全局作用域**求值的 —— 被包进函数作用域后全部 `is not defined`:
  - 校验永远不通过 → 输入框反复重弹 → 用户只能点取消 → 脚本拿到 `null` → `if (year === null) return` → **提示「已取消导入」**。
  - 受影响面:77 个带校验函数的 prompt 调用点里 **62 个**是这种写法。
  官方实现是**原样执行**(`ShiguangWarehouse.resolveScript` 只读文件,`EduImportBrowserUi.runOriginalImportScript` 直接 `evaluateJavascript(script, null)`),顶层 `function` / `var` / `const` / `let` 都能被后续脚本看到。现在改为原样注入(垫片 + 脚本)。
- 随之移除只为包装脚本而存在的 `reportError`,并把控制台输出留在 logcat(`ImportWebView` tag)以便排查。

### 测试
- 新增 A/B 对照回归测试:取**东北大学真实的 `validateYearInput`**,分别按「原样执行」与「旧的 IIFE 包裹」注入,断言前者是全局函数且合法输入通过、后者为 `undefined` 且合法输入被判 `not defined`(即复现本缺陷)。桥契约测试共 41 项,全部通过。

## 0.1.11 (2026-09-19)

对照上游实现(拾光仓库配套 App),把导入桥这一层按官方行为重写了一遍,补齐三处偏差:

### 修复
- **「已取消导入」的第三处成因(0.1.8 引入的回归)**:垫片每次执行都会写 `window.__resolvers = {}`,而这个表是「进行中的 Promise」的唯一索引;0.1.8 为了保险又在 `onPageFinished` 注入了一次垫片,于是**正在等待用户操作的弹窗回调会被清空**——那个 Promise 再也无法 resolve,只能挂到 60 秒超时返回 null,适配器就判成「用户取消」。现在垫片与官方一样幂等:`if (window._shiguangBridgeInjected) return;`。
- **桥回调可能发到了错误的 WebView**:主页面与 `window.open` 出来的弹窗页是两个**独立 JS 文档**,各自的 `__resolvers` 相互隔离。现在开始导入前先 `bindWebView(目标页)` 再注入脚本(官方 `runOriginalImportScript` 同样是先绑定后注入),弹窗 / 单选 / 输入框的结果一定回到脚本所在的那个文档。
- 单选列表「无可选项」时直接返回 `null`(官方行为):此前会返回 `defaultIndex`,而适配器普遍校验 `idx >= list.length`(HPU、WUST 等),一个不存在的序号会被判成「已取消导入」。
- 校验判定与官方 `validateEduBridgePrompt` **逐字对齐**:`false` / `null` / `undefined` / 空串 = 通过;其它值取 `String()` 作为错误文案;校验函数抛异常时回显异常信息(此前把 `false` 判成失败,且异常时放行)。

### 测试
- 桥契约测试扩到 36 项:除参数归一化 / 返回编码外,新增「垫片重复注入不得清空等待中的 Promise」(即上面的回归)与用**真实适配器**的 `validate*` 函数验证校验语义。

## 0.1.10 (2026-09-19)

### 修复
- **点「执行导入」后仍提示「已取消导入」、课程始终导不进来(真正的根因)**:`showPrompt` 的校验函数语义写反了。拾光 Bridge 规范是「**校验通过返回 `false`,不通过返回错误文案**」——BTBU 的注释明确写了「规范要求:验证通过返回 false,失败返回错误文案」,GXDLXY / HNVCC / CCIT / CQRK / CQIE 的实现也都如此。而我们按「返回 `false` 即不通过」判定,于是**输入合法反而被判失败** → 弹框反复重弹 → 用户只能点取消 → 脚本拿到 `null`,走 `if (year === null) return;` 之类的分支 → 提示「已取消导入」。已用三个真实适配器的校验函数实测:合法输入时它们确实返回 `false`。
  现在只把「返回非空字符串」判为不通过,并把该文案显示在重弹的弹框里(适配器返回文案本来就是为了展示给用户)。
- 与 0.1.9 的单按钮 `showAlert` 是同一条链路上的两处缺陷:0.1.9 之前是「确定 / 取消」两按钮里点取消会中止(55 个适配器受影响),这次是校验语义反了导致永远过不了校验。两处叠加才是「怎么点都导不进来」。

## 0.1.9 (2026-09-19)

### 修复
- **点「执行导入」弹出「取消导入」且什么都没导入**:桥把 `showAlert` 实现成了「确定 / 取消」两按钮,取消与点击弹窗外都会返回 false。而拾光 Bridge 协议里 `showAlert(titleText, contentText, confirmText)` 是**只有 confirmText 一个按钮**的提示框,官方固定返回 true;适配器普遍写成 `const ok = await showAlert(...); if (!ok) return;`(234 个脚本中有 **55 个**是这样),于是点了取消或误触外部,脚本就静默退出——课程一条都不会导入,也没有任何失败提示。现在改回单按钮并禁止取消(与协议一致)。
- 移除协议中并不存在的 `showConfirmDialog`(上一版误加,全部适配器均无调用),避免再出现「两个按钮里有一个会让脚本中止」的情况。
- `showSingleSelection` 的 items 若传数组,改为在垫片内先 `JSON.stringify`:WebView 把 JS 数组传给 Java 的 String 形参只会 `toString()` 成逗号串,原生按 JSON 解析会失败并立即返回、选择框根本不弹出。内置的 240 处调用目前都会自己序列化,所以这是与官方 polyfill 对齐的防御性修正。

## 0.1.8 (2026-09-19)

### 修复
- **导入页白屏(多种成因,逐一补齐)**:① `window.open` / `target=_blank` 打开的新窗口此前完全无人接管——很多教务系统把课表放在新窗口里,点下去毫无反应、看起来就是白屏,现在开启多窗口并用子 WebView 承载弹窗(可关闭、可返回),适配脚本与桥回调都会作用于「用户当前看到的那一页」;② 渲染进程被系统回收(内存紧张/内核崩溃)后不恢复,页面永久白屏只能杀 App,现在收到 `onRenderProcessGone` 会销毁并重建 WebView、回到原地址;③ 统一身份认证(CAS)跨域跳转缺少第三方 Cookie,登录后会在空白页之间打转,现在放开;④ 证书校验失败默认被静默取消(整页全白且毫无提示),现在取消并说明原因,子资源证书问题不遮挡整页;⑤ 补齐 `databaseEnabled`、`useWideViewPort`、`loadWithOverviewMode`、`setInitialScale` 等固定宽度教务页面与老 WebSQL 页面所需的设置。
- **桌面 UA 版本号写死会白屏**:以前固定成 `Chrome/120.0.0.0`,网站会按该版本下发较新的 JS,而本机内核跑不了就整页白屏;现在改用 `WebSettings.getDefaultUserAgent` 取**真实内核版本**,只把平台标识换成桌面。
- 非 http(s) 链接(`tel:` / `intent:` 等)改由系统处理,不再让 WebView 自己加载而留下空白页;WebView 控制台输出接入 logcat,便于白屏时定位。

## 0.1.7 (2026-09-19)

### 修复
- 删除课程后详情页不自动关闭:导航依赖的「已删除」标志在仓库调用**返回之后**才置位,而该调用里串了云同步(网络),慢或失败时课程早已从本地删掉、页面却一直不关。现在详情页改为响应式——课程从列表消失即自动返回,从详情页或编辑页删除都生效;云同步同时移到后台,保存 / 删除不再等网络。
- 顶部周次计算错误:周视图读的是持久化的「当前周」,只在保存学期 / 导入 / 点「今天」时写入,启动、回到前台、跨天都不会重算;且周次以「开学日当天」为锚,而星期列按周一对齐,非周一开学的学期会整体错位。现在统一按**开学日所在周的周一**实时推算,周视图 / 课程详情排期 / 桌面小组件 / 上课提醒四处同源;`‹ ›` 改为临时翻看,「今天」归零。
- 导入时出现莫名其妙的报错提示:上一版为了不让脚本报错静默而挂了全局 `window.onerror` / `unhandledrejection`,结果把**教务网页自身**的脚本报错也当成适配器错误弹给用户。现在只上报适配脚本自身抛出的异常。
- 适配器桥接参数错位:各适配器的 `showAlert` 有 3 参 / 4 参两种写法,桥按固定形参取值会把 callbackId 认错,Promise 一直挂到 60s 超时。现在垫片先把实参补齐到固定个数,再追加 callbackId。
- `showPrompt` 忽略校验函数:适配器传入的 `validateYearInput` 等校验函数此前被忽略,非法输入会直接进入后续流程;现在校验不通过会带着原输入重新弹出。
- `showSingleSelection` 忽略默认项:改为单选列表并预选 `defaultIndex`;另为需要双按钮的适配器(HUAT 等)补上 `showConfirmDialog`。
- 导入静默丢课:自定义时间课程(无节次号)此前会被直接丢弃,且解析失败一律返回空列表。现在支持 `isCustomTime` / `customStartTime` / `customEndTime`,并明确提示「收到 N 条、跳过 M 条」或解析失败原因。
- 适配器数据陈旧、部分学校选不出方案:内置适配器停留在旧快照(146 校 / 156 适配器),上游已迭代到 **218 校 / 242 适配器(索引协议 v2)**;其中黑龙江大学的适配器因预编译缺陷被整条丢弃——在索引里能看到学校、点进去却没有可选方案,自然导不了。现已同步上游最新数据并修好预编译(见「变更」)。

### 改进
- 作息时间:导入识别到的作息 / 开学日期不再静默覆盖本地设置,改为在导入确认页展示(节数与起始节时间)由用户勾选是否应用;作息编辑页新增**自动生成时间线**(开始时间 + 单节时长 + 课间 + 节数),并在保存前校验节次顺序与时间重叠。
- 安装包瘦身:开启 R8 混淆 + 资源压缩,只打 `arm64-v8a` / `armeabi-v7a`,裁剪内置思源宋体(14.1MB → 3.3MB,保留 GB2312 全字集),移除 assets 中从不读取的 147 个 YAML 索引文件,并限制依赖库语言资源。
- 检查更新:服务端挑选安装包时优先 release 资产,未配置签名时不会再有可能把 debug 包推给用户。

### 变更
- 适配器仓库刷新到上游最新:146 校 / 156 适配器 → **218 校 / 242 适配器**(新增南开、中山、哈工大、西交、郑大等 72 所学校;上游索引已升到 v2 协议,但 YAML 字段不变,预编译与 App 侧无需改动)。新增的学校目录与脚本一并入库。
- `tools/yaml2json.mjs` 修两处解析缺陷:① 不识别 `\"` 转义,含转义引号的适配器描述被从引号处截断;② 列表项写在第 0 列时**整所学校的适配器全部被丢弃**(黑龙江大学即为此)。现按任意缩进解析。
- 新增适配器数据自洽校验 `node tools/verify-warehouse.mjs`:索引里的学校必须有适配器、每个适配器引用的脚本必须存在、`adapter_id` 唯一、未被引用的脚本必须为 0(现已全部通过;仅上游自检目录 `GLOBAL_TOOLS` 引用了不存在的 `test.js`,该目录 App 本就不展示)。
- 新增 `tools/subset-font.py`(字体子集化,可按需复现)。

## 0.1.6 (2026-09-19)

### 新增
- 设置 → 适配器同步:从自建 Netlify 站点拉取最新教务适配器(学校索引 + 全部适配脚本),落地后导入页优先使用云端数据,失败或未同步时自动回退应用内置;可一键恢复内置。
- 设置 → 检查更新:应用内检查新版本(经反代查询 GitHub Release)、下载 APK 并拉起系统安装器;启动时按 24 小时节流静默检查,有新版弹非阻断提示。

### 修复
- 教务导入 WebView 未设 User-Agent,部分教务系统按 UA 返回移动版页面导致脚本解析不到课表,现使用桌面 Chrome UA。
- 适配脚本报错完全静默(注入回调为 null):现包裹 try/catch,并挂 `onerror` / `unhandledrejection`,异常以中文提示弹出。
- 桥 Promise 无超时,页面切换或原生未回调时永久挂起:新增 60s 超时兜底。
- 适配器索引/脚本读取失败会静默返回空列表:现缓存优先读取,两者皆失败时展示可操作错误面板,可直接跳转「适配器同步」。
- 通用教务方案的自定义网址未校验:自动补全 https 协议并校验主机名,无效网址不再进入 WebView。
- 返回上一步再进入登录页会重建 WebView 丢失登录态:同一适配器复用 WebView,仅在切换适配器时重建。
- 配置变更/进程重建时保存非 Parcelable 的选择对象可能崩溃:`SchoolEntry` / `AdapterEntry` 改为 Parcelable。

### 变更
- Netlify 站点新增静态发布目录(构建期由 `tools/build-netlify.mjs` 生成 `warehouse/bundle.json`,供客户端适配器同步);反代新增 `/version`(最新版本信息)与 `/apk`(流式代理安装包)接口。

## 0.1.5 (2026-08-30)

### 修复
- 教务导入 WebView 白屏：Android 9+ 默认禁止访问 http 明文页面，而多数教务系统入口仍是 http，加载被系统静默拦截导致白屏无提示。现在放行教务系统明文访问，并新增加载失败提示（无法解析域名 / 连接超时 / SSL 证书失败 / http 被禁等会显示中文原因，不再无声空白）。

## 0.1.4 (2026-08-30)

### 修复
- 课程节次设置：原来只能选「1-2 / 3-4 / 5-6」两节一组的大节，无法单独设置某一节。改为逐节点选：点一节为单节，再点一节组成任意连续区间（如 1、3、2-4）。
- 「设置 → 学期周次」设置不生效：打开页面时表单在设置加载完成前就被默认值填充，已保存的开学日期显示为「未设置」，再次保存会覆盖清空。现在等待真实设置到达后再回填（仅一次，不覆盖用户编辑）；保存开学日期后自动把当前周校准为真实周数，周视图与单双周课程立即按新学期生效。
- 教务导入 WebView：刷新按钮不再跳回入口地址，而是刷新当前页。

### 改进
- 教务导入第 2 步（WebView 登录页）顶部信息区压缩为两行，把更多空间留给网页；底部新增后退 / 前进 / 刷新控件（无可退/可进历史时置灰）。

## 0.1.3 (2026-08-26)

### 修复
- 「提醒可靠性」页：精确闹钟行在权限**已授予**时点击无响应。现在无论是否已授予，点击都会跳转到系统「闹钟和提醒」特殊权限页（未授予弹授权页、已授予展示开关状态），与通知、电池白名单等行行为一致。
- 荣耀 MagicOS 10：「自启动」入口原来错误跳转到「权限管理」页。改用荣耀官方 action（`hihonor.intent.action.HSM_STARTUPAPP_MANAGER`）启动，直达「应用启动管理」页（可切换自动/手动管理，控制自启动、关联启动、后台活动）。

### 改进
- 自启动状态显示：荣耀等国产 ROM 不提供公开 API 检测真实自启动状态，改为「进过应用启动管理页即视为已配置」的引导标记（持久化、仅本机）。首次引导与「提醒可靠性」页状态一致，配置过之后显示「已开启」。
- 课程提醒/权限引导集中改造：Android 16 Live Updates 前台服务（分段进度条 + 状态栏小字）、提醒触发时读取当堂最新课程、后台启动受限时回退普通高优先级通知；修复提醒不弹出根因（重排闹钟 + 取消窗口旧闹钟）；onboarding 由弹窗改为全屏逐项引导。
