rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 每个用户只能读写自己的 users/{uid} 子树
    match /users/{userId}/{document=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
